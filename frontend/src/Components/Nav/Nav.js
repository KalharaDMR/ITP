import React from 'react';
import './Nav.css';

const Nav = () => {
    return (
        <nav className="nav-panel">
            <span className="hotel-name">NATURE'S LAKE VIEW</span>
        </nav>
    );
};

export default Nav;